{
  "fmt": {
    "options": {
      "singleQuote": true
    }
  },
  "tasks": {
    "test": "deno test -A",
    "test-coverage": "deno test -A --no-check --coverage=coverage && deno coverage --lcov --output=lcov.info coverage && code-coverage"
  },
  "compilerOptions": {
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
